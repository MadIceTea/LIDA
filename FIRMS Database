var dataset = ee.ImageCollection('FIRMS').filter(
    ee.Filter.date('2018-03-29', '2018-04-08'));
var fires = dataset.select('T21');
var firesVis = {
  min: 325.0,
  max: 400.0,
  palette: ['red', 'orange', 'yellow'],
};
Map.addLayer(fires, firesVis, 'Fires');
